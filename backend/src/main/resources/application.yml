spring:
  application:
    name: ai-knowledge-copilot
  profiles:
    default: default

  flyway:
    enabled: false

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
